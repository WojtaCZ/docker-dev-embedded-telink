{
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "_comment": "Layer 20 — merged over 00-baseline.json by entrypoint.sh. This leaf inherits from docker-dev-template directly, so it must add the `fetch` server itself (embedded-base's layer 10 is not in this chain).",
    "mcpServers": {
        "fetch": {
            "command": "uvx",
            "args": ["mcp-server-fetch"]
        }
    }
}
